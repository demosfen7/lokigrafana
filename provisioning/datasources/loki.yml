apiVersion: 1

datasources:
  - name: Loki
    type: loki
    access: proxy
    # доступ к Loki по имени сервиса из docker-compose, а не по IP
    url: http://loki:3100
    isDefault: true
    editable: false
